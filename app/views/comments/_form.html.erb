<%= form_for([@link, @comment]) do |f| %>
  <%= f.hidden_field :parent_id %>
  <div class="field">
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
